# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local environment. These hold real Supabase credentials — never commit them.
.env
.env.*
!.env.example

# Scratch / tooling artifacts
.docx_tmp/

# Agent harness state (memory, sessions, SQLite index). Local only — the index
# is a binary that rewrites on every session, so tracking it is pure churn.
.nova/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
